function setup() {
    let link = document.createElement("link");
    link.setAttribute("id", "windowIcon")
    link.setAttribute("rel", "icon");
    link.setAttribute("href", "CP.png");
    document.head.append(link);
}
setup();
